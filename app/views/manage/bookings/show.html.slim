= render partial: 'manage/bookings/navigation'
= render partial: BookingStrategy::Default::Template.template_path('manage/bookings/booking'), locals: { booking: @booking }

section
  = link_to t(:back), manage_bookings_path, class: 'btn btn-default'
