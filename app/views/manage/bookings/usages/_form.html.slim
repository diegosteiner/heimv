= form_with(model: [:manage, @booking, @usage], local: true) do |f|
  = f.error_summary

  fieldset
    = f.hidden_field :tarif_id

    = f.collection_select :tarif_id, @booking.home.tarifs, :id, :label
    = f.number_field :price_per_unit, step: 0.01
    = f.number_field :used_units, step: 0.01
    = f.check_box :committed
    = f.text_area :remarks

  .form-actions.pt-4.mt-3
    = f.submit
