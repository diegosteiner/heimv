- current_organisation.homes.where(requests_allowed: true).each do |home|
  .card.my-3
    .card-body
      h1.text-center= home
      = react_component('calendar/OccupancyCalendar', { homeId: home.id, baseUrl: url_for(only_path: false) })
