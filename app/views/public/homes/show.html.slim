h1.mt-2.text-center[id=@home.id]
  = @home
  
= react_component('calendar/OccupancyCalendar', { \ 
    calendarUrl: calendar_home_occupancies_url(org: current_organisation&.slug, locale: I18n.locale, home_id: @home.to_param, format: :json), \
    occupancyAtUrl: at_occupiable_calendar_url(org: current_organisation&.slug, locale: I18n.locale, occupiable_id: @home.to_param, date: '__DATE__'), \
    defaultView: current_organisation&.settings&.default_calendar_view \
  })
