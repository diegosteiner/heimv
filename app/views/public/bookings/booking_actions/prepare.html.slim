= render partial: 'manage/bookings/navigation', locals: { booking: @booking }

= render partial: @booking_action.to_partial_path('prepare'), locals: { invoke_path: public_booking_invoke_action_path(@booking.token), back_path: public_booking_path(@booking.token) }

