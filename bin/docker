#!/bin/sh

TARGET_ENV=$1
shift

case "$TARGET_ENV" in
  -d | --dev | dev)
    DEFAULT="up"
    docker-compose "${@:-$DEFAULT}";;
  -p | --production | production )
    docker-compose -f docker-compose.yml -f docker-compose.production.yml $@;;
  -t | --test | test )
    DEFAULT="run --use-aliases test"
    docker-compose ${@:-$DEFAULT};;
  * ) break ;;
esac
