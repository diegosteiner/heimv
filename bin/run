#!/bin/sh

case "$1" in
  -d | --dev | dev)
    docker-compose -f docker-compose.yml -f docker-compose.development.yml up ;;
  -p | --production | production )
    ;;
  -t | --test | test )
    docker-compose -f docker-compose.yml -f docker-compose.test.yml run web bin/check ;;
  * ) break ;;
esac
